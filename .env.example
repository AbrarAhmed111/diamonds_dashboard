# Helix API — used ONLY on the server (src/app/api/helix/route.ts).
# These are NOT prefixed with NEXT_PUBLIC, so they are never exposed to the browser.
HELIX_API_URL=https://helix.diamondpigs.com/api/v1
HELIX_API_KEY=your_api_key
